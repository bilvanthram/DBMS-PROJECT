spring.application.name=reportflow
server.port=8081

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/reportdb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Bilvanth@1317}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

# MongoDB Atlas (reports). Prefer setting MONGODB_URI as an env var rather than committing credentials.
spring.data.mongodb.uri=${MONGODB_URI:mongodb://admin:admin123@ac-iianpsh-shard-00-00.gvhsf1k.mongodb.net:27017,ac-iianpsh-shard-00-01.gvhsf1k.mongodb.net:27017,ac-iianpsh-shard-00-02.gvhsf1k.mongodb.net:27017/?ssl=true&replicaSet=atlas-o6j5x4-shard-0&authSource=admin&appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:reportflow}

# One-time: copy existing PostgreSQL reports into MongoDB on startup. Set true, run once, set back to false.
app.migrate-legacy-reports=${MIGRATE_LEGACY_REPORTS:false}

app.jwt.secret=${JWT_SECRET:reportflow-jwt-secret-key-change-this-in-production-2026}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
